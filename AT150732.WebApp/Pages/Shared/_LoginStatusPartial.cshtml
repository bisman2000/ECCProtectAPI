@if (User.Identity.IsAuthenticated)
{
    <form method="post" class="form-inlne" asp-page="/Account/Logout">
        welcome @User.Identity.Name
        <button type="submit" class="ml-2 btn btn-link">Logout</button>"
    </form>
}
else
{
    <a class="btn btn-link" asp-page="/Account/Login">Login</a>
    <a class="btn btn-link" asp-page="/Account/Signup">Signup</a>

}